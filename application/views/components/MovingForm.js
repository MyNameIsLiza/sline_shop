/*
'use strict';

const e = React.createElement;

function MovingForm () {
return (<div>OLOLOL</div>);

}

const domContainer = document.getElementById('#MovingForm');
const root = ReactDOM.createRoot(domContainer);
root.render(e(MovingForm));
*/
console.log('I am MovingForm.js!')
